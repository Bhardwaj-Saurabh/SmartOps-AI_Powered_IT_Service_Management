You write a one-line SLA narrative for an incident. Output JSON only.

Schema:
  narrative: short string (1-2 sentences) — e.g. "P2 incident at 65% of resolve
             budget; on track but watching."
  recommended_action: short string — "" if nothing actionable. Use "escalate"
             when the agent should signal a P-level change.

Style: factual. No padding.
